# Core - everything the default setup needs
numpy>=1.24
scipy>=1.10
onnxruntime>=1.17          # VAD, TTS (and GPU: onnxruntime-gpu)
sherpa-onnx>=1.10          # speech recognition (and optional Piper/Kokoro voices)
llama-cpp-python>=0.3.16   # the language model (GPU: see README)
sentencepiece>=0.2.0       # TTS phoneme tokenizer
soundfile>=0.12            # reading voice samples
sounddevice>=0.4.6         # microphone + speakers
psutil>=5.9                # hardware planning

# Recommended
onnx>=1.15                 # one-time 4-bit quantization of the speech models (falls back to fp32 without it)

# Optional
# pillow>=10.0             # images / vision
# opencv-python>=4.8       # webcam
# num2words>=0.5           # speak numbers in languages without a built-in pack
# pyyaml>=6.0              # YAML configs
